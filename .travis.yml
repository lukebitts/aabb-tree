language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly

script:
    - |
        cargo build

after_success:
  if [ "$TRAVIS_RUST_VERSION" == "nightly" ]; then
    cargo test --feature "test-tools"
  fi
